---
layout: page
title: Home
nav_order: 1
---

This page uses `layout: page`, so it shows up in the sidebar nav automatically.

Pages are one-offs (like this one) or indexes of a collection (like
[Stuff]({{ '/stuff/' | relative_url }})). Give a page `nav_order` to control
where it lands in the nav; pages without one sort last, alphabetically.
